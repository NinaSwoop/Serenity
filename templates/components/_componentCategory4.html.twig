{% include "/components/_progressbar_category4.html.twig" %}

<div class="my-4 mx-4">
	<p class="m-3">Afin de sécuriser votre retour à la maison votre chirurigien vous invite à prendre rendez-vous avec les professionnels de santé suivants :</p>
	{% for userMedicalDiscipline in userMedicalDisciplines %}
		<p class="professionalBox d-flex justify-content-between align-items-center">
			<span>{{ userMedicalDiscipline.medicalDiscipline.name}}</span>
			<a href="{{ path("app_medical_discipline_check" , { id: userMedicalDiscipline.medicalDiscipline.id }) }}">
				<i class="bi {{ userMedicalDiscipline.isChecked ? 'bi-check-circle' : 'bi-circle'}}"></i>
			</a>
		</p>
	{% endfor %}
</div>
