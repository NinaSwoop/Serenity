{% include "/components/_progressbar.html.twig" %}

<div class="container p-4 my-4">
	{% for checklist in category.checklists %}
		<div class="card shadow-sm bg-body rounded mb-4">
			<div class="card-body">
                <p class="d-flex justify-content-between align-items-center">{{ checklist.name }}<a href="#"><img class ="empty-checked-relative" src="{{ asset('build/images/checked-empty.svg')}}" alt="..."></a></p>
                <p class="text-muted mb-0">{{ checklist.description }}</p>
            </div>
		</div>
	{% endfor %}
</div>
