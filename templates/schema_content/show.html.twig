{% extends 'base.html.twig' %}

{% block title %}SchemaContent{% endblock %}

{% block body %}
    <h1>SchemaContent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ schema_content.id }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ schema_content.picture }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ schema_content.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_schema_content_index') }}">back to list</a>

    <a href="{{ path('app_schema_content_edit', {'id': schema_content.id}) }}">edit</a>

    {{ include('schema_content/_delete_form.html.twig') }}
{% endblock %}
