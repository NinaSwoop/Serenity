{% extends 'base.html.twig' %}

{% block title %}MedicalDiscipline{% endblock %}

{% block body %}
    <h1>MedicalDiscipline</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ medical_discipline.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ medical_discipline.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_medical_discipline_index') }}">back to list</a>

    <a href="{{ path('app_medical_discipline_edit', {'id': medical_discipline.id}) }}">edit</a>

    {{ include('medical_discipline/_delete_form.html.twig') }}
{% endblock %}
