<form method="post" action="{{ path('app_medical_course_delete', {'id': medical_course.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ medical_course.id) }}">
	<a class="btn btn btn-danger m-2">Supprimer</a>
</form>
