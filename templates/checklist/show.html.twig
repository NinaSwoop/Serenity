{% extends 'base.html.twig' %}

{% block title %}Checklist{% endblock %}

{% block body %}
    <h1>Checklist</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ checklist.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ checklist.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ checklist.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_checklist_index') }}">back to list</a>

    <a href="{{ path('app_checklist_edit', {'id': checklist.id}) }}">edit</a>

    {{ include('checklist/_delete_form.html.twig') }}
{% endblock %}
